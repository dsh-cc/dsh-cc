<!-- GENERATED from docs/claude-code-capabilities.yaml — do not edit; run pnpm docs:parity -->

# Claude Code parity matrix

Generated from the machine-readable capability manifest. One symbol per capability,
derived from four orthogonal dimensions (`recognized` / `mounted` / `behavioral` / `ux`):

- ✅ full parity: behavior matches upstream, mounted by default, complete UX
- 🔶 partial: usable with known differences (see Deviation/Notes)
- ❌ missing: absent today
- 🚫 not a parity port — may exist as a dsh-native equivalent (see Deviations)

## Engine subsystems

| Status | Capability | Recognized | Mounted | Behavior | UX | Evidence | Deviation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ✅ | <a id="cap-engine.agent-loop"></a>Agent loop + tool pipeline | ✓ | ✓ | Full | Full | [s1](packages/core/tools/src/cc-names.ts) [t1](packages/core/tools/tests/cc-names.spec.ts) | — | — |
| ✅ | <a id="cap-engine.ask-user-question"></a>AskUserQuestion | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/bundle/cc-shell/tests/ask-user-bundle.spec.ts) [t2](packages/ui/tui/tests/driver-question.spec.ts) | — | — |
| 🔶 | <a id="cap-engine.bash-jobs"></a>Bash / PowerShell + background jobs | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [s2](packages/preset/cc/agent.cordis.yml) | downgrade — CC's TaskCreate/TaskOutput/TaskStop tool naming is not aliased; the dsh jobs tools (tool-jobs over the host job registry) carry the equivalent surface under dsh names. | — |
| 🔶 | <a id="cap-engine.context-compression"></a>CCR tool-output compression (context-crusher) | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/context/context-crusher/tests/composition.spec.ts) | divergent — Not an upstream CC surface: a dsh-cc extension shipped flag-gated (enabled: false, mode: dry-run by default) so savings are measured before any replacement. | — |
| ✅ | <a id="cap-engine.coordinator"></a>Coordinator mode | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/subagent/coordinator/tests/coordinator.spec.ts) | — | — |
| 🔶 | <a id="cap-engine.cost-tracking"></a>Cost / token tracking | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/session/command-cost/tests/command-cost.spec.ts) [t2](packages/ui/tui/tests/usage-panel.spec.ts) | downgrade — Ships a starter price table of official list prices for common DeepSeek/GLM/Kimi models (composition decides pricing; deployment billing may differ), and CC quota/limit surfaces are vendor-billing-bound — the TUI /usage panel footnotes quota as unavailable. | — |
| ❌ | <a id="cap-engine.credentials"></a>Credentials storage | — | — | Missing | Missing | — | downgrade — FORCED DOWNGRADE at seed: the row is genuinely ✅ via the host-plane upstream package dsh-credentials-local, but that package has no in-repo file to anchor a positive claim against, so per the evidence backfill rule the claim is downgraded until an in-repo seam exists. | — |
| ✅ | <a id="cap-engine.file-tools"></a>File tools (Read/Edit/Write), Glob/Grep | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [s2](packages/preset/cc/agent.cordis.yml) | — | — |
| 🔶 | <a id="cap-engine.ide-lsp"></a>IDE integration / LSP | ✓ | — | Partial | Partial | [t1](packages/bundle/cc-shell/tests/lsp-bundle.spec.ts) | downgrade — Two paths. Production: Serena MCP via user-scope ~/.claude.json (v1.7.0 pin) — symbol-level retrieval/editing/diagnostics, ~30 tools deferred through ToolSearch; gaps: no hover tool, no call hierarchy, no push diagnostics. Native dsh-lsp/dsh-lsp-stdio/dsh-tool-lsp exist with a closed 4-operation read-only seam but are NOT mounted by any shipped composition (no lsp rows in cordis.patch.yml/agent.cordis.yml). cc-plugin-loader does not parse lspServers/.lsp.json manifests; PreToolUse hooks ignore additionalContext. /ide and editor pods remain host-shell scope; headless does not apply. | — |
| ✅ | <a id="cap-engine.notebook-edit"></a>NotebookEdit | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/core/tool-notebook-edit/tests/tools.spec.ts) | — | — |
| ❌ | <a id="cap-engine.notifications"></a>Notifications (bell/OS/iterm) | — | — | Missing | Missing | — | upstream-blocked — No notification seam in deepseek-harness; needs a new design. | — |
| 🔶 | <a id="cap-engine.onboarding"></a>Onboarding / tips | ✓ | ✓ | Divergent | Partial | [s1](packages/ui/tui/src/harness/onboarding.ts) [s2](packages/ui/tui/src/harness/driver-run-local.ts) [t1](packages/ui/tui/tests/onboarding.spec.ts) | divergent — Deliberate shape change from Claude Code's first-run wizard: the gate is derived (no theme or trust step) — the provider panel opens iff the boot default-model seed settles with `selection.current === undefined` on a TTY, dismissal keeps the no-model notice, and the `cc-onboarding.suppressed` settings section (cleared by `/onboard`) is the only stored flag. Contextual tips are not implemented. | — |
| ✅ | <a id="cap-engine.plan-mode"></a>Plan mode | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/ui/tui/tests/plan-channel.spec.ts) [t2](packages/interaction/command-permissions/tests/plan-channel.integration.spec.ts) | — | — |
| 🔶 | <a id="cap-engine.reasoning-fold-probe"></a>Reasoning-fold probe (Stage 0, ledger only) | ✓ | ✓ | Partial | Missing | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/llm-tuning/reasoning-fold/tests/component.spec.ts) | divergent — dsh-cc extension: read-only, ledger-only probe (cc-reasoning-fold settings namespace, probe: true default); no chunk is ever rewritten and no model call can fail because of it. | — |
| ❌ | <a id="cap-engine.remote-sessions"></a>Remote / web sessions | — | — | Missing | Missing | — | downgrade — FORCED DOWNGRADE at seed: the legacy row reads "🔶 different — CC bridge/ is claude.ai-bound; dsh has its own web/host/sdk/acp stack outside this repo", but that stack has no in-repo surface to anchor a positive claim against, so the seed records the four dimensions as non-positive until the external stack gains an in-repo seam. | — |
| ❌ | <a id="cap-engine.sandbox"></a>Sandbox | — | — | Missing | Missing | — | downgrade — FORCED DOWNGRADE at seed: the row is genuinely ✅ via the host-plane upstream packages dsh-sandbox-local + policy, but they have no in-repo file to anchor a positive claim against, so per the evidence backfill rule the claim is downgraded until an in-repo seam exists. | — |
| 🔶 | <a id="cap-engine.schedule"></a>Schedule / reminders | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/bundle/cc-shell/tests/schedule-bundle.spec.ts) | downgrade — after_seconds / at / every_seconds (>=300s) supported; CC's cron-expression selectors are unsupported until the dsh-schedule selector seam lands (upstream extension planned). | — |
| ✅ | <a id="cap-engine.sleep"></a>Sleep tool | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/core/tool-sleep/tests/tools.spec.ts) | — | — |
| ✅ | <a id="cap-engine.structured-output"></a>StructuredOutput (synthetic output tool) | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/core/tool-structured-output/tests/tools.spec.ts) | — | — |
| 🔶 | <a id="cap-engine.subagent-handoff"></a>Subagent handoff store (handoff_put / handoff_get) | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [s2](packages/plugin/dsh-cc-agents/agents/critic.md) [t1](packages/subagent/handoff-store/tests/store.spec.ts) [t2](packages/subagent/handoff-store/tests/agent-surface.tripwire.spec.ts) | divergent — dsh-cc extension: the advisory-threshold contract (cc-handoff settings namespace, default 8192) is enforced only by the agents' prompt contract, not by the tool — put never rejects on size, and the cheap-lane shunt agents deliberately keep the pair out of their whitelists. | — |
| ✅ | <a id="cap-engine.tool-search"></a>ToolSearch (deferred tools) | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/core/tool-search/tests/tool-search.spec.ts) | — | — |
| 🔶 | <a id="cap-engine.tui"></a>Vim mode / keybindings / ghost text | ✓ | ✓ | Partial | Partial | [t1](packages/ui/tui/tests/mode-cycle.spec.ts) | downgrade — dsh --profile tui is the terminal surface: Shift+Tab mode cycling, Esc interrupt, Ctrl+C/Ctrl+S/Ctrl+T/Ctrl+O/Up/Tab bindings, approval previews with 1/2/3 answers on a shared FIFO, leading-! bash mode with separate history, TUI-local /export-md /copy /usage, six-role theme, context-occupancy statusline, custom statusLine and multi-hunk diff transcript rendering. Vim mode and ghost text are still later; the statusLine contract's remaining limits are detailed in ux.statusline. | — |
| 🚫 | <a id="cap-engine.vendor-internals"></a>Buddy / KAIROS / undercover / ultraplan / computer-use | — | — | Missing | Missing | — | non-goal — Anthropic-internal or vendor-bound features; no design asset and out of parity scope. | — |
| 🚫 | <a id="cap-engine.voice"></a>Voice | — | — | Missing | Missing | — | non-goal — Vendor feature with no design asset; won't port. | — |
| 🔶 | <a id="cap-engine.web-fetch"></a>WebFetch | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [s2](packages/bundle/cc-shell/cordis.patch.yml) [t1](packages/core/tool-web-fetch/tests/tools.spec.ts) [t2](packages/interaction/permission-rules/tests/domain.spec.ts) [t3](packages/bundle/cc-shell/tests/web-fetch-http-patch.spec.ts) | downgrade — Mounted via @dsh-cc/tool-web-fetch (optional prompt summarized on resolve('haiku'); prompt without haiku hard-fails before fetch); the tool upgrades http: URLs to https: before the seam, CC-style. Fetch executor is the stock dsh-base @deepseek-ai/dsh-web-fetch-http provider with CC egress caps restated in cc-shell — upstream rc.1 resolves public-only addresses and pins them per redirect hop, closing the DNS-rebinding residual the retired literal gate left. WebFetch(domain:) content rules match hostnames. Residual: no Anthropic preflight, no 15-min cache, no pre-approved docs domains, no private-network escape hatch. | — |
| ✅ | <a id="cap-engine.web-search"></a>WebSearch | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) | — | — |
| ✅ | <a id="cap-engine.workflow-ralph"></a>Workflow / Ralph loop | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [s2](packages/preset/cc/agent.cordis.yml) | — | — |

## Hook events

| Status | Capability | Recognized | Mounted | Behavior | UX | Evidence | Deviation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 🔶 | <a id="cap-hooks.executors"></a>Hook executors (command/http/prompt/agent) | ✓ | — | Partial | Partial | [t1](packages/hooks/hooks-claude-code/tests/executors.spec.ts) [s1](packages/hooks/hooks-claude-code/src/dispatch.ts) | downgrade — command + http (SSRF-allowlisted via allowedHttpHookUrls) executors are always on; prompt/agent executors are gated behind enablePromptHooks / enableAgentHooks (default off) and resolve their model: through ccModelRoutes (omitted model → the haiku cheap lane). | mounted: command+http executors always on; prompt/agent behind enablePromptHooks/enableAgentHooks, default off (spec §6) |
| ❌ | <a id="cap-hooks.message-display"></a>MessageDisplay hook | — | — | Missing | Missing | — | downgrade — Not bridged — no dsh emit point for message-display rendering events. | — |
| 🔶 | <a id="cap-hooks.notification"></a>Notification hook | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/events.spec.ts) | downgrade — Bridged for the permission_prompt subtype only; idle_prompt / auth_success / elicitation* subtypes have no equivalent seam in a headless harness (cannot map). | — |
| ✅ | <a id="cap-hooks.permission-denied"></a>PermissionDenied hook | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/events.spec.ts) | — | — |
| ✅ | <a id="cap-hooks.permission-request"></a>PermissionRequest hook | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/events.spec.ts) | — | — |
| ✅ | <a id="cap-hooks.post-compact"></a>PostCompact hook | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/events.spec.ts) | — | — |
| ❌ | <a id="cap-hooks.post-tool-batch"></a>PostToolBatch hook | — | — | Missing | Missing | — | downgrade — Not bridged — the dsh tool pipeline has no per-batch emit point yet (hook-bridge follow-up). | — |
| ✅ | <a id="cap-hooks.post-tool-use"></a>PostToolUse hook | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/events.spec.ts) | — | — |
| ✅ | <a id="cap-hooks.post-tool-use-failure"></a>PostToolUseFailure hook | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/events.spec.ts) | — | — |
| ❌ | <a id="cap-hooks.pre-compact"></a>PreCompact hook | — | — | Missing | Missing | — | upstream-blocked — Needs an upstream compaction-waterfall interception seam in dsh-compaction (planned). | — |
| 🔶 | <a id="cap-hooks.pre-tool-use"></a>PreToolUse hook | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/events.spec.ts) | downgrade — Bridged with matcher support and the permissionDecision decision contract, but additionalContext is ignored, bounding hook-based pre-tool shaping. | — |
| ✅ | <a id="cap-hooks.session-end"></a>SessionEnd hook | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/events.spec.ts) | — | — |
| ✅ | <a id="cap-hooks.session-start"></a>SessionStart hook | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/events.spec.ts) | — | — |
| 🔶 | <a id="cap-hooks.setup"></a>Setup hook | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/events.spec.ts) | downgrade — Bridged as a first-run approximation, not the full upstream Setup contract. | — |
| ✅ | <a id="cap-hooks.stop"></a>Stop hook | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/coverage-stop.spec.ts) | — | — |
| ✅ | <a id="cap-hooks.subagent-start"></a>SubagentStart hook | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/background-subagent-start.spec.ts) | — | — |
| ✅ | <a id="cap-hooks.subagent-stop"></a>SubagentStop hook | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/events.spec.ts) | — | — |
| ✅ | <a id="cap-hooks.task-created"></a>TaskCreated hook | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/events.spec.ts) | — | — |
| ❌ | <a id="cap-hooks.user-prompt-expansion"></a>UserPromptExpansion hook | — | — | Missing | Missing | — | downgrade — Not bridged — no dsh emit point for prompt-template expansion. | — |
| ✅ | <a id="cap-hooks.user-prompt-submit"></a>UserPromptSubmit hook | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/events.spec.ts) | — | — |

## Command surface

| Status | Capability | Recognized | Mounted | Behavior | UX | Evidence | Deviation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 🔶 | <a id="cap-commands.agents"></a>/agents | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-agents/tests/command-agents.spec.ts) | downgrade — Slice 0 MVP: thin snapshot (list/detail/stop) over listChildren + agents + resumePinStore; groups are residency-only (Working / Idle / Ready — no Blocked/Done); /agents attach is a reserved, unimplemented namespace. The TUI local-slash surface consumes the same ccAgents snapshot and ADDS fold-derived decorations (provider, prompt excerpt, last stopReason) to the detail view; the thin preset surface vs the decorated TUI surface is a deliberate divergence, never different ids or ordering. | — |
| 🔶 | <a id="cap-commands.branch"></a>/branch | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-branch/tests/command-branch.spec.ts) | downgrade — Forks and reports the child id; switching to the child requires a restart. | — |
| ✅ | <a id="cap-commands.cache-health"></a>/cache-health | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/observability/cache-health/tests/composition.spec.ts) | — | — |
| ✅ | <a id="cap-commands.compact"></a>/compact | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-compact/tests/command-compact.spec.ts) | — | — |
| 🔶 | <a id="cap-commands.config"></a>/config | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-config/tests/command-config.spec.ts) | downgrade — Text-only render/patch with an allowlisted key set, not an interactive editor. | — |
| ✅ | <a id="cap-commands.cost"></a>/cost | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/session/command-cost/tests/command-cost.spec.ts) | — | — |
| ✅ | <a id="cap-commands.diff"></a>/diff | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-diff/tests/command-diff.spec.ts) | — | — |
| ✅ | <a id="cap-commands.doctor"></a>/doctor | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-doctor/tests/command-doctor.spec.ts) [t2](packages/interaction/command-doctor/tests/collect-json.spec.ts) | — | — |
| 🚫 | <a id="cap-commands.exit"></a>/exit | ✓ | — | Divergent | Missing | [s1](packages/ui/tui/src/slash.ts) | non-goal — Host-owned by design; the dsh-native equivalents are the TUI /exit command (packages/ui/tui/src/slash.ts) and the idle double Ctrl+C gesture, so a preset-side port is out of parity scope. | — |
| ✅ | <a id="cap-commands.export"></a>/export | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/session/command-export/tests/command-export.spec.ts) | — | — |
| ✅ | <a id="cap-commands.help"></a>/help | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-help/tests/command-help.spec.ts) [s2](packages/interaction/command-usage/src/index.ts) [t2](packages/interaction/command-usage/tests/usage.spec.ts) [t3](packages/ui/tui/tests/slash-help.spec.ts) | — | behavioral: dsh-cc extension — every slash command (harness-mounted and TUI-local) accepts a trailing `help` (or -h/--help) argument rendering its usage, parameters, and subcommands via @dsh-cc/command-usage; the /help index appends a tip advertising it. |
| 🔶 | <a id="cap-commands.init"></a>/init | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-init/tests/command-init.spec.ts) | downgrade — Drives a follow-up turn that writes/refreshes CLAUDE.md rather than the upstream one-shot initializer flow. | — |
| ✅ | <a id="cap-commands.learn"></a>/learn [apply\|all\|days=N] | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/session/command-learn/tests/command-learn.spec.ts) | — | — |
| ✅ | <a id="cap-commands.mcp"></a>/mcp | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-mcp/tests/command-mcp.spec.ts) | — | — |
| ✅ | <a id="cap-commands.memory"></a>/memory | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-memory/tests/command-memory.spec.ts) | — | — |
| 🚫 | <a id="cap-commands.model"></a>/model | ✓ | — | Divergent | Missing | [s1](packages/ui/tui/src/slash.ts) [t1](packages/ui/tui/tests/driver-model.spec.ts) | non-goal — Host-owned by design; the dsh-native equivalents are the TUI-local /model and /effort commands (packages/ui/tui/src/slash.ts, model-catalog) plus the ccModelRoutes alias service, so a preset-side port is out of parity scope. | — |
| ✅ | <a id="cap-commands.output-style"></a>/output-style | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/compat/cc-output-styles/tests/output-styles.spec.ts) | — | — |
| ✅ | <a id="cap-commands.permissions"></a>/permissions [mode] | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-permissions/tests/command-permissions.spec.ts) [t2](packages/ui/tui/tests/driver-permissions.spec.ts) | — | — |
| ✅ | <a id="cap-commands.plan"></a>/plan | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/ui/tui/tests/plan-channel.spec.ts) | — | — |
| 🔶 | <a id="cap-commands.plugin"></a>/plugin [subcommand] | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-plugin/tests/command-plugin.spec.ts) [t2](packages/interaction/command-plugin/tests/manage.spec.ts) | downgrade — No interactive menu UI (text output only); a static trust-warning line instead of interactive consent prompts; CC-only subcommands not implemented (details/eval/init/prune/tag/validate, --config, --sparse, managed scope). State surface diverges from upstream: listing presents the dual-home merged view (claude home read-visible, dsh entries win per key), mutations write to the dsh home (`$DSH_HOME` / `~/.dsh`) never the Claude home, and a shadowed-claude marketplace re-add prints a one-line notice. | — |
| ✅ | <a id="cap-commands.provider"></a>/provider | ✓ | ✓ | Full | Full | [s1](packages/ui/tui/src/provider-command.ts) [s2](packages/ui/tui/src/provider-flow.ts) [s3](packages/ui/tui/src/provider-presets.ts) [t1](packages/ui/tui/tests/driver-provider-panel.spec.ts) [t2](packages/ui/tui/tests/driver-provider-actions.spec.ts) [t3](packages/ui/tui/tests/driver-provider-refresh.spec.ts) | — | — |
| ✅ | <a id="cap-commands.release-notes"></a>/release-notes | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-release-notes/tests/command-release-notes.spec.ts) | — | — |
| ✅ | <a id="cap-commands.reload-plugins"></a>/reload-plugins | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/bundle/cc-shell/tests/ccPlugins.spec.ts) | — | — |
| ✅ | <a id="cap-commands.rename"></a>/rename <title> | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-rename/tests/command-rename.spec.ts) | — | — |
| 🔶 | <a id="cap-commands.resume"></a>/resume | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-resume/tests/command-resume.spec.ts) | downgrade — Lists sessions; switching is host-owned (dsh --resume <id>). | — |
| ❌ | <a id="cap-commands.rewind"></a>/rewind | — | — | Missing | Missing | — | upstream-blocked — Needs the session file-snapshot seam; no checkpoint/rewind design exists yet. | — |
| ✅ | <a id="cap-commands.skills"></a>/skills | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-skills/tests/command-skills.spec.ts) | — | — |
| ✅ | <a id="cap-commands.stats"></a>/stats | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/session/command-stats/tests/command-stats.spec.ts) | — | — |
| ✅ | <a id="cap-commands.status"></a>/status | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-status/tests/command-status.spec.ts) | — | — |
| 🔶 | <a id="cap-commands.tasks"></a>/tasks | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-tasks/tests/command-tasks.spec.ts) | downgrade — Human-facing /tasks lists background jobs only; the todo-list seam is pending. The model-facing dsh-tool-todo and the TUI Ctrl+T todo panel cover the rest of the workflow. The footer cross-links to /agents (N background agents — /agents for details) when the ccAgents snapshot service resolves. | — |
| ✅ | <a id="cap-commands.version"></a>/version | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/interaction/command-version/tests/command-version.spec.ts) | — | — |

## Sessions and context

| Status | Capability | Recognized | Mounted | Behavior | UX | Evidence | Deviation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ❌ | <a id="cap-sessions.checkpointing"></a>File checkpointing and rewind | — | — | Missing | Missing | — | upstream-blocked — Feature absent; requires a snapshot seam in the session layer. | — |
| ✅ | <a id="cap-sessions.persistence"></a>Session persistence / resume / fork | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [s2](packages/ui/tui/src/exit-tip.ts) [s3](packages/ui/tui/src/harness/resumed-cwd-guard.ts) [t1](packages/ui/tui/tests/resumed-cwd-guard.spec.ts) [t2](packages/compat/session-title-provider/tests/provider.spec.ts) [t3](packages/ui/tui/tests/session-list.spec.ts) | — | — |

## Memory and CLAUDE.md

| Status | Capability | Recognized | Mounted | Behavior | UX | Evidence | Deviation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ❌ | <a id="cap-memory.claude-md-imports"></a>CLAUDE.md @path imports | — | — | Missing | Missing | — | downgrade — The @path import machinery is absent — no parser or loader handles imports today. | — |
| 🔶 | <a id="cap-memory.core"></a>Memory / CLAUDE.md | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [s2](packages/preset/cc/agent.cordis.yml) [t1](packages/memory/memory/tests/save.spec.ts) [t2](packages/memory/memory/tests/team.spec.ts) | downgrade — Per-repo isolation (worktrees/subdirs share one store), shared global layer, memory_save as the save channel, recall reference-doc suppression, and opt-in team memory all work; CC's mtime-threaded recall and memoryAge aging are deferred until the harness FsInfo seam grows an mtime field. | — |

## Skills

| Status | Capability | Recognized | Mounted | Behavior | UX | Evidence | Deviation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 🔶 | <a id="cap-skills.system"></a>Skills system | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/skill/skill-claude-code/tests/conditional-bundled.spec.ts) [t2](packages/ui/tui/tests/driver-skill-slash.spec.ts) | downgrade — Loader + base tool-skill, CC paths conditional activation, bundled subset (debug/simplify/batch), and TUI /name routing all work; CC's verify/stuck skills are not ported (ant-only; verify companion files absent). | — |

## Subagents

| Status | Capability | Recognized | Mounted | Behavior | UX | Evidence | Deviation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 🔶 | <a id="cap-subagents.task-tool"></a>Subagents / Agent tool / teams | ✓ | ✓ | Divergent | Partial | [s1](packages/preset/cc/agent.cordis.yml) [s2](packages/preset/cc/agent.cordis.yml) [t1](packages/subagent/task/tests/tool.spec.ts) [t2](packages/subagent/task/tests/epoch-collector.spec.ts) [t3](packages/subagent/task/tests/suppress-settled.spec.ts) [t4](packages/subagent/task/tests/integration.spec.ts) [t5](packages/subagent/task/tests/registry-publication.spec.ts) [s3](packages/subagent/task/src/preload-tools.ts) [t6](packages/subagent/task/tests/preload-tools.spec.ts) [t7](packages/ui/tui/tests/promote-key.spec.ts) [t8](packages/subagent/resume-pins/tests/gate.spec.ts) [t9](packages/bundle/cc-shell/tests/resume-pins-composition.spec.ts) [s4](packages/compat/cc-plugin-loader/src/agents.ts) [s5](packages/subagent/task/src/plugin-agents.ts) [t10](packages/subagent/task/tests/plugin-agents.spec.ts) [t11](packages/compat/cc-plugin-loader/tests/dsh-cc-agents.spec.ts) [s6](CLAUDE.md) [s7](packages/plugin/dsh-cc-agents/skills/dsh-cc-agents-orchestration/SKILL.md) [s8](packages/subagent/resume-pins/src/plugin.ts) | divergent — subagent_type dispatch over per-workspace .claude/agents (persona = definition systemPrompt, alias routing via ccModelRoutes, sanitized toolFilter, maxDepth 3, bundled explore/dsh-cc-guide agents, the bundled explore additionally carrying read-only serena symbol retrieval tools mcp__serena__find_symbol / find_referencing_symbols / get_symbols_overview) plus the continuable background loop (run_in_background, agentId-addressable send_message/interrupt/list). Deliberate deviations: fork + background is rejected naming upstream issue #2124 (fork stays foreground one-shot); no TaskOutput alias and no outputFile field; omitting run_in_background stays foreground unless the definition pins background: true, unlike Claude Code's interactive omit=background. Pinned defaults (repo-guarded): the official plugin agent dsh-cc-agents:critic pins background: true, so omitting run_in_background on those Task calls launches them continuable in the background (the call resolves { status: 'async_launched' } immediately and the real result arrives later as a wake message — no inline result to compose on); run_in_background: false forces synchronous collection. Deliberate deviation on pins: dsh-cc-agents:executor and dsh-cc-agents:marathon ship NO pin — a mutating agent defaults to foreground so the delegator verifies its report before composing — and there is NO global omit=background default either: the bundled explore and dsh-cc-guide agents stay unpinned and keep foreground first-epoch collect. Foreground collect refit: non-fork foreground calls (named agents and general-purpose) collect the child's first epoch INLINE as a continuable child — same pin preallocation, pre-start pin write, and tombstone-on-throw as the background path, so a foreground-launched child is pinnable/resumable exactly like a background one. The collector is a dsh-cc-INTERNAL parity instrument (bus watcher over subagent/start + subagent/end, one shared listener pair and watch map, matched by first-epoch runId; docs/plans/ 2026-09-05-epoch-collector-dsh-cc.md), NOT a Claude Code mechanism — declared deviations: (1) for inline-collected epochs the settlement account lives only in the tool result, not the parent session log (the duplicated subagent-settled notice is dropped by a pop-once pre-step filter; a promoted child is removed from the set and delivers normally); (2) on exec.signal abort the child is interrupted aborted exactly once via ctx.subagents.interrupt({kind:'ancestor'}) and the tool call resolves PROMPTLY with a synthetic stopReason "aborted" (never awaiting child quiescence; the real terminal arrives only as the suppressed notice); (3) a seam without interrupt degrades to a prompt resolve without child interrupt. Output schema gains optional backgroundedByUser: true (set by the Slice 3 promotion path, never by Slice 2 code paths). Ctrl+B promotion (TUI-only surface): while the dialog is busy AND the root-realm ccCollectorRegistry (published by the cc-subagent-task plugin, CcPlugins pattern) has ≥1 armed foreground collect for THIS session AND CLAUDE_CODE_DISABLE_BACKGROUND_TASKS is unset, Ctrl+B promotes EVERY armed collect of the session: the pending tool call resolves { status: 'async_launched', agentId, backgroundedByUser: true }, the watch is released (the runId-matched end later resolves nothing), the child is REMOVED from the suppression set so its settlement notice delivers exactly once normally, and the TUI echoes one status line. Pre-acceptance promotion (Ctrl+B fired while startContinuable is still in flight) resolves async_launched as soon as start resolves. Repeated presses are idempotent (a promoted collect unregisters itself; the second press is a consumed no-op). Declared deviations: promotion is a TUI-only surface — non-TUI clients have NO promotion path; busy Esc/Ctrl+C still interrupts (every registered collect of the session aborts exactly-once with a prompt synthetic aborted resolution); idle Ctrl+B is untouched (the editor's tui.editor.cursorLeft binding); under tmux, Ctrl+B is the default prefix — double-press passes a literal Ctrl+B through (send-prefix) and the Ctrl+X Ctrl+B chord is P1 (not implemented). Env kill switch CLAUDE_CODE_DISABLE_BACKGROUND_TASKS: a non-empty value other than case-insensitive 0/false disables the backgrounding-by-default pins (omissions collect in foreground) — explicit run_in_background true/false arguments remain honored both ways. Deliberate partial-parity deviation: upstream Claude Code's exact precedence for this env var is unverified, so the argument precedence is declared rather than asserted as parity. Capacity guard: the Task tool refuses a new continuable child (background OR foreground collect) when the parent already has 25 live children (listChildren entries with activity running) with the actionable error "parent has 25 live subagents; /agents stop <id> to release one, or let children settle" — a dsh-cc-side safety valve; Claude Code has no equivalent surfaced limit. Continuable cold resume: each background spawn captures a resume pin (resolveDetailed-selected alias plus maxTokens) into the resumePinStore service (preset row cc-resume-pins, isolated as resumePinStore in cc-services), and resume restores the pinned tuple INCLUDING explicit absence (a pin recording an unset reasoningEffort/maxTokens is honored as absent rather than re-resolved); a pinned model the parent can no longer resolve is gated by the subagents-resume settings namespace (subagents-resume.onUnavailableModel: block\|route-current, default block) with deny codes SUBAGENT_MODEL_UNAVAILABLE / WORKSPACE_CHANGED / DEFINITION_CHANGED / PINNED_TOOL_UNAVAILABLE / PIN_ORPHANED / PIN_UNREADABLE naming the policy knob — Claude Code has no equivalent mechanism. Spawn-time preload (dsh-cc-side, no Claude Code equivalent): an explicit `tools:` entry that is still a deferred MCP tool is activated through the duck-typed ctx.toolSearch seam at spawn, BEFORE the child starts, on BOTH dispatch paths — activation is process-global (every admitting agent's schema grows after the spawn); wildcard entries are restrict-only, deny/sanitize-excluded names are never activated, eager names are skipped, and outcomes surface in the Task result text (Preloaded deferred tools for child / Not preloaded: name (reason)). Plugin-agent dispatch (dsh-cc-side, CC-parity): plugin agents are dispatched by CC-style scoped ids `plugin:agent` — file definitions and scoped ids occupy disjoint name spaces (file definitions whose agentType contains `:` are skipped with a warning so the scoped-id namespace stays unambiguous) and bare plugin names are not addressable, matching Claude Code. Plugin definitions are enumerated live from the `subagents` seam and folded identically to file definitions (persona, sanitized toolFilter, alias-resolved model, maxDepth 3, background pin, deferred-MCP preload, foreground collect / background continuable); dispatch targets the `spawn` provider, so the AgentProvider fork-inheritance semantics are not used. Mounting a plugin's agents without a namespacePrefix leaves them undiscoverable to the Task tool (back-compat escape hatch, not a supported dispatch mode). First-party exercise: the official dsh-cc-agents plugin (packages/plugin/dsh-cc-agents, shipped via the repo-root `dsh-cc` marketplace) dispatches by scoped id end-to-end — dsh-cc-agents:critic / dsh-cc-agents:executor mount from the real plugin dir and are pinned by plugin-agents.spec.ts and the loader's dsh-cc-agents.spec.ts. Delegation discipline is prompt-taught across the Task description, the background-loop system-prompt section, both spawn-result texts, the resume-pins send_message advisory and list_agents annotations, the repo CLAUDE.md, and the dsh-cc-agents orchestration skill: one task, one instance — send_message continues only the child's current assignment, and every new task is a fresh subagent_fork spawn. | — |

## MCP

| Status | Capability | Recognized | Mounted | Behavior | UX | Evidence | Deviation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ✅ | <a id="cap-mcp.client"></a>MCP client | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/mcp/mcp-client/tests/mcp-client.spec.ts) [t2](packages/mcp/mcp-client/tests/auth.spec.ts) [t3](packages/mcp/mcp-client/tests/registry.spec.ts) [t4](packages/bundle/cc-shell/tests/mcp-ready-notice.spec.ts) [t5](packages/bundle/cc-shell/tests/mcp-gating.spec.ts) | — | — |
| 🔶 | <a id="cap-mcp.config-discovery"></a>MCP config discovery precedence | ✓ | ✓ | Divergent | Partial | [s1](packages/preset/cc/agent.cordis.yml) [s2](packages/preset/cc/agent.cordis.yml) [s3](packages/bundle/cc-shell/src/index.ts) [t1](packages/bundle/cc-shell/tests/mcp-gating.spec.ts) [t2](packages/mcp/mcp-config/tests/paths.spec.ts) [t3](packages/mcp/mcp-config/tests/migrate.spec.ts) [t4](packages/interaction/command-mcp/tests/command-mcp.spec.ts) | divergent — Deliberate dsh-first rule with no Claude Code analog: when a dsh-native config (the project .mcp.json or $DSH_HOME/.mcp.json) declares at least one server, Claude Code MCP config files are not loaded; skipped claude-only servers surface via a logger warn, a one-shot session-start TUI notice, and a self-clearing /mcp status line, and /mcp migrate imports them into $DSH_HOME/.mcp.json (raw entries verbatim, atomic write, .bak backup, existing names win). Escape hatch: mcpLoadClaudeFiles: true on cc-shell-glue restores the old all-merge behavior; an explicit mcpConfigFiles list bypasses gating entirely. | — |
| ✅ | <a id="cap-mcp.serena-first"></a>Serena-first prompt steering | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/compat/cc-serena-first/tests/serena-first.spec.ts) | — | — |

## Plugins and marketplaces

| Status | Capability | Recognized | Mounted | Behavior | UX | Evidence | Deviation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 🔶 | <a id="cap-plugins.loader"></a>Plugin system | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [s2](packages/compat/cc-plugin-loader/src/index.ts) [s3](packages/bundle/cc-shell/src/mcpSeam.ts) [s4](packages/bundle/cc-shell/src/index.ts) [s5](packages/plugin/dsh-cc-agents/.claude-plugin/plugin.json) [s6](packages/hooks/hooks-claude-code/src/index.ts) [s7](packages/compat/cc-plugin-loader/src/seams.ts) [s8](packages/compat/cc-plugin-loader/src/hooks.ts) [s9](packages/preset/cc/agent.cordis.yml) [t1](packages/hooks/hooks-claude-code/tests/plugin-hooks-seam.spec.ts) [t2](packages/bundle/cc-shell/tests/leaked-services.spec.ts) [t3](packages/compat/cc-plugin-loader/tests/mounts.spec.ts) [t4](packages/compat/cc-plugin-loader/tests/discovery.spec.ts) [t5](packages/compat/cc-plugin-loader/tests/dsh-cc-agents.spec.ts) [t6](packages/bundle/cc-shell/tests/mcp-seam.spec.ts) [t7](packages/bundle/cc-shell/tests/mcp-seam.integration.spec.ts) [t8](packages/compat/cc-plugin-manager/tests/install-dsh-cc-agents.spec.ts) [t9](packages/plugin/dsh-cc-agents/tests/package-shape.spec.ts) | divergent — Discovery presents the dual-home merged view: the Claude home ($CLAUDE_CONFIG_DIR / ~/.claude) stays read-visible for Claude Code compatibility while the dsh home ($DSH_HOME / ~/.dsh) is the write root, merged per key with dsh-wins. The fork is one-way (real Claude Code sees only its own home) and ids dsh has taken over stop tracking later claude-side changes; a claudeHome-alone caller keeps legacy single-root behavior. | mounted: Exercised end-to-end by a first-party plugin: packages/plugin/ dsh-cc-agents (the official critic + executor + marathon agents plus an orchestration skill) ships via the repo-root official `dsh-cc` marketplace (.claude-plugin/marketplace.json) and is pinned by the real-dir mount spec, the install→cache→mount round-trip spec, and the package-shape spec. The mcp component is mounted through the cc-mcp-seam child plugin provided by the cc-shell glue (built from the loader's own mountMcpServers path — see docs/plans/2026-09-07-plugin-mcp-seam.md; a 2026-09-07 probe discovered the previous full/none claim here overstated a dead feature, corrected once the seam was wired). The hooks component is now likewise mounted: since 2026-09-08 the hooks-claude-code bridge provides the loader's `hooks` seam (the same seam-probe pattern; the plugin's pluginRoot is passed through for ${CLAUDE_PLUGIN_ROOT} substitution — see docs/plans/2026-09-08-plugin-hooks-seam.md). Known limitations: the mcpServers tally counts register-time loads, so config-skipped servers (unset env var, malformed entry) surface only via logger warns, not the tally (a richer registerServer return is a follow-up); OAuth is not supported for plugin-declared servers; a plugin server with the same name shadows a same-name .mcp.json server (plugins mount first and mcp-client rejects duplicate names). |
| 🔶 | <a id="cap-plugins.management"></a>Plugin management state | ✓ | ✓ | Divergent | Partial | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/compat/cc-plugin-manager/tests/merged-state.spec.ts) [t2](packages/compat/cc-plugin-manager/tests/paths.spec.ts) [t3](packages/compat/cc-plugin-manager/tests/alignment.spec.ts) [t4](packages/compat/cc-plugin-manager/tests/marketplace.spec.ts) [t5](packages/interaction/command-plugin/tests/manage.spec.ts) [t6](packages/compat/cc-plugin-manager/tests/install.spec.ts) [t7](packages/bundle/cc-shell/tests/ccPluginManager.spec.ts) | divergent — State root split: every /plugin mutation writes under the dsh home ($DSH_HOME / ~/.claude never written), the Claude home stays read-visible for compatibility, and both homes merge per key with dsh-wins (null tombstones hide claude-only marketplaces). Claude Code reads only its own home, so the fork is one-way; ids materialized into the dsh installed file stop tracking later claude-side changes (takeover staleness). Setting CLAUDE_CONFIG_DIR no longer relocates writes; a claudeHome-alone caller keeps legacy single-root behavior. | — |

## Settings

| Status | Capability | Recognized | Mounted | Behavior | UX | Evidence | Deviation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ✅ | <a id="cap-settings.hot-reload"></a>Settings hot reload | ✓ | ✓ | Full | Full | [t1](packages/settings/settings-cascade/tests/watcher.spec.ts) [t2](packages/settings/settings-cascade/tests/watcher.spec.ts) | — | — |
| 🔶 | <a id="cap-settings.migrations"></a>Settings migrations | ✓ | ✓ | Partial | Partial | [t1](packages/settings/settings-migrations/tests/migrations.spec.ts) | downgrade — Mechanism only (version-gated runMigrations over an atomically-written settings.json, auto-run on mount) — no real migrations yet. | — |
| ✅ | <a id="cap-settings.precedence"></a>Settings precedence | ✓ | ✓ | Full | Full | [t1](packages/settings/settings-cascade/tests/cascade.spec.ts) [t2](packages/settings/settings-cascade/tests/merge.spec.ts) | — | — |

## Permissions

| Status | Capability | Recognized | Mounted | Behavior | UX | Evidence | Deviation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 🔶 | <a id="cap-permissions.rules"></a>Permission rules | ✓ | ✓ | Partial | Partial | [t1](packages/interaction/permission-rules/tests/mode.spec.ts) [t2](packages/interaction/permission-rules/tests/classifier.spec.ts) [t3](packages/interaction/permission-rules/tests/auto-stage.spec.ts) [t4](packages/interaction/permission-rules/tests/listener-auto-stage.spec.ts) [t5](packages/interaction/permission-rules/tests/llm-classifier.spec.ts) [t6](packages/interaction/permission-rules/tests/llm-classifier.spec.ts) [t7](packages/interaction/permission-rules/tests/auto-stage.spec.ts) [t8](packages/ui/tui/tests/driver-classifier-breaker.spec.ts) [s1](packages/interaction/permission-rules/src/auto-stage.ts) [t9](packages/settings/settings-cascade/tests/auto-mode.spec.ts) [t10](packages/ui/tui/tests/approval-preview.spec.ts) [t11](packages/ui/tui/tests/driver-default-mode.spec.ts) [t12](packages/interaction/permission-rules/tests/permission-rules.spec.ts) | downgrade — Rule engine + dangerous-command/path risk classifier, durable modes (default/acceptEdits/plan/auto/bypassPermissions via /permissions), plan deny with exit_plan_mode guidance, bypassPermissions pinning danger-full-access with resumeSandbox restore, and the always-allow derived-rule persistence all work. `auto` mode additionally gains an opt-in, escalate-only LLM risk-classifier stage: a one-shot auxiliary model verdict over tool name + rendered parameters (never tool results, input wrapped as an explicit DATA block the model is instructed never to repeat, quote, or follow), fail-to-ask on every classifier failure, `$defaults` soft-deny prose expansion, a verdict LRU cache, and durable `permission/classifier` session audit events (armed only when enabled, an llm service is mounted, and the alias route resolves). Read-only tool calls are exempt: they never invoke the classifier (the legacy auto path applies). Verdict parsing stays strict and fail-closed — a malformed output yields the constant reason "classifier output unparseable" (model output never reaches the modal; the audit record is digest-only). A per-route consecutive-failure circuit breaker (threshold 3, keyed provider/model) opens the stage for a failing lane: no further classifier calls on that route, one warn per process, one `breaker` audit event per session, reset by a settings change (rebuild). The breaker is restart-durable within a session: the first call of a session seeds the per-route streak from the durable log (attributed events only; a restored streak ≥ threshold opens at seed time). An open breaker surfaces one visible TUI fallback notice per session (live-appended `breaker` events only — never replayed logs), and caller-cancelled classifications are tagged `cancelled`, never breaker-counted. Raw model output is debuggable via the opt-in `DSH_PERMISSION_CLASSIFIER_DEBUG=1` process-log channel (`[dsh:classifier:raw]`, truncated to 2 KiB — never session events). Defaults: `timeoutMs` 8000, verdict budget 1024 tokens. The UI honors `permissions.defaultMode`: statusline, session switch, Shift+Tab cycle start, and the /permissions picker all fall back to the live merged settings default (fold precedence — a recorded session mode still wins), with the cycle clamped to its members. Settings-surface deviation: the auto-mode section is delivered as `permissions.autoMode` — an `autoMode` key inside the existing `permissions` namespace (camelCase key in a kebab namespace, like `defaultMode`), NOT Claude Code's root-level `autoMode` key, because top-level settings namespaces must be kebab-case upstream. Other documented CC auto-mode divergences (explicit ask rules still proxied to allow in auto; same-source allow-before-deny) remain recorded as unfixed follow-ups — they are not addressed or masked here. Remaining vs CC: managed/enterprise remote settings, UI mode cycle; the upstream permission/mode catalog pin is an upstream polish. | — |

## Models

| Status | Capability | Recognized | Mounted | Behavior | UX | Evidence | Deviation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 🔶 | <a id="cap-models.aliases"></a>Model aliases | ✓ | ✓ | Partial | Partial | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/compat/cc-model-aliases/tests/resolveAlias.spec.ts) [t2](packages/compat/cc-model-aliases/tests/service.spec.ts) | downgrade — Frontmatter aliases (sonnet/opus/haiku/fable + dsh lanes + open set), settings model-aliases overlay with null-delete and builtin fallback, shared toAgentOptions per-field inheritance, and the ccModelRoutes service all work. Follow-ups: the /model command, ANTHROPIC_* env vars (no Anthropic semantics), and aliasing the main-session default model. | — |

## Workspace

| Status | Capability | Recognized | Mounted | Behavior | UX | Evidence | Deviation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ✅ | <a id="cap-workspace.worktree"></a>Worktree tools | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/workspace/tool-git-worktree/tests/worktree.spec.ts) | — | — |

## Interactive UX

| Status | Capability | Recognized | Mounted | Behavior | UX | Evidence | Deviation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ✅ | <a id="cap-ux.output-styles"></a>Output styles | ✓ | ✓ | Full | Full | [s1](packages/preset/cc/agent.cordis.yml) [t1](packages/compat/cc-output-styles/tests/styles.spec.ts) | — | — |
| 🔶 | <a id="cap-ux.statusline"></a>Status line | ✓ | ✓ | Partial | Partial | [s1](packages/ui/tui/src/harness/statusline-wiring.ts) [s2](packages/ui/tui/src/harness/statusline-command.ts) [s3](packages/settings/settings-cascade/src/cc-key-aliases.ts) [s4](packages/ui/tui/src/harness/statusline-settings.ts) [s5](packages/ui/tui/src/harness/statusline-payload.ts) [s6](packages/ui/tui/src/harness/usage-view.ts) [t1](packages/ui/tui/tests/driver-statusline.spec.ts) [t2](packages/ui/tui/tests/statusline-command.spec.ts) [t3](packages/ui/tui/tests/statusline-settings.spec.ts) [t4](packages/ui/tui/tests/statusline-payload.spec.ts) [t5](packages/ui/tui/tests/statusline-cc-transcript.spec.ts) [t6](packages/settings/settings-cascade/tests/cc-key-aliases.spec.ts) | downgrade — The command's stdout is rendered as multiple status-line rows capped at 3 (CC renders every row as its own status-line row); when the custom statusline is active, dsh-cc appends one client-drawn row below the command output carrying the permission mode + key hints (`<mode> · shift+tab · /quit`) — the stdin payload carries no permission-mode field (CC's doesn't either; CC renders that row as its own chrome). A 60 s hard run cap and a 64 KiB stdout cap are enforced (CC documents no timeout), and no debug-mode failure output is logged for the statusline command. The stdin payload omits fields with no truthful runtime source: version, output_style.name, worktree, workspace.git_worktree, workspace.repo, session_name, prompt_id, fast_mode, thinking, vim, agent, pr, cost.total_cost_usd / total_api_duration_ms / total_lines_* — context_window.current_usage IS emitted with CC field names (input_tokens / output_tokens / cache_creation_input_tokens / cache_read_input_tokens), truthfully sourced from the harness token usage projection's last-step buckets; steps cancelled or failed mid-flight may leave partial usage in that snapshot (documented staleness). used_percentage / remaining_percentage are derived from the currentUsage context length when present (fallback: the projected pressure sample; omitted when neither source exists). display_name duplicates model.id; exceeds_200k_tokens uses the currentUsage context length and is omitted when no occupancy source exists. transcript_path points at the dsh-cc-maintained CC-shape plaintext transcript mirror when it is ready (see ux.statusline-cc-transcript). hideVimModeIndicator is parsed but behaviorally inert (no vim mode yet); disableAllHooks / allowManagedHooksOnly gating keys are unimplemented. External settings.json edits apply at the next boot (no file watcher; in-process settings commits react immediately). refreshInterval is honored in seconds with a minimum clamp of 1. | — |
| 🔶 | <a id="cap-ux.statusline-cc-transcript"></a>CC-shape plaintext transcript mirror for the custom statusline | ✓ | ✓ | Divergent | Partial | [s1](packages/ui/tui/src/harness/statusline-cc-transcript.ts) [t1](packages/ui/tui/tests/statusline-cc-transcript.spec.ts) | divergent — When the custom statusline is active, dsh-cc maintains a CC-shape plaintext transcript mirror at $DSH_HOME/tui/cc-transcripts/<sessionId>.jsonl (assistant usage lines + user anchor lines, atomic rebuild on bind/rebind, seq-watermarked appends) and the statusline payload's transcript_path advertises the mirror only once it is ready (>=1 usage line), instead of the dsh zstd-compressed private-format session artifact that CC-shape consumers cannot parse. The mirror is a filtered projection (usage + anchor lines only), not a full transcript. Hook payloads (packages/hooks/hooks-claude-code/src/payloads.ts) still point transcript_path at the dsh session artifact. | — |

## Deviations and known limits

- <a id="dev-engine.bash-jobs"></a>`engine.bash-jobs` — downgrade: CC's TaskCreate/TaskOutput/TaskStop tool naming is not aliased; the dsh jobs tools (tool-jobs over the host job registry) carry the equivalent surface under dsh names.
- <a id="dev-engine.context-compression"></a>`engine.context-compression` — divergent: Not an upstream CC surface: a dsh-cc extension shipped flag-gated (enabled: false, mode: dry-run by default) so savings are measured before any replacement.
- <a id="dev-engine.cost-tracking"></a>`engine.cost-tracking` — downgrade: Ships a starter price table of official list prices for common DeepSeek/GLM/Kimi models (composition decides pricing; deployment billing may differ), and CC quota/limit surfaces are vendor-billing-bound — the TUI /usage panel footnotes quota as unavailable.
- <a id="dev-engine.credentials"></a>`engine.credentials` — downgrade: FORCED DOWNGRADE at seed: the row is genuinely ✅ via the host-plane upstream package dsh-credentials-local, but that package has no in-repo file to anchor a positive claim against, so per the evidence backfill rule the claim is downgraded until an in-repo seam exists.
- <a id="dev-engine.ide-lsp"></a>`engine.ide-lsp` — downgrade: Two paths. Production: Serena MCP via user-scope ~/.claude.json (v1.7.0 pin) — symbol-level retrieval/editing/diagnostics, ~30 tools deferred through ToolSearch; gaps: no hover tool, no call hierarchy, no push diagnostics. Native dsh-lsp/dsh-lsp-stdio/dsh-tool-lsp exist with a closed 4-operation read-only seam but are NOT mounted by any shipped composition (no lsp rows in cordis.patch.yml/agent.cordis.yml). cc-plugin-loader does not parse lspServers/.lsp.json manifests; PreToolUse hooks ignore additionalContext. /ide and editor pods remain host-shell scope; headless does not apply.
- <a id="dev-engine.notifications"></a>`engine.notifications` — upstream-blocked (upstream_dependency: notification-seam): No notification seam in deepseek-harness; needs a new design.
- <a id="dev-engine.onboarding"></a>`engine.onboarding` — divergent: Deliberate shape change from Claude Code's first-run wizard: the gate is derived (no theme or trust step) — the provider panel opens iff the boot default-model seed settles with `selection.current === undefined` on a TTY, dismissal keeps the no-model notice, and the `cc-onboarding.suppressed` settings section (cleared by `/onboard`) is the only stored flag. Contextual tips are not implemented.
- <a id="dev-engine.reasoning-fold-probe"></a>`engine.reasoning-fold-probe` — divergent: dsh-cc extension: read-only, ledger-only probe (cc-reasoning-fold settings namespace, probe: true default); no chunk is ever rewritten and no model call can fail because of it.
- <a id="dev-engine.remote-sessions"></a>`engine.remote-sessions` — downgrade: FORCED DOWNGRADE at seed: the legacy row reads "🔶 different — CC bridge/ is claude.ai-bound; dsh has its own web/host/sdk/acp stack outside this repo", but that stack has no in-repo surface to anchor a positive claim against, so the seed records the four dimensions as non-positive until the external stack gains an in-repo seam.
- <a id="dev-engine.sandbox"></a>`engine.sandbox` — downgrade: FORCED DOWNGRADE at seed: the row is genuinely ✅ via the host-plane upstream packages dsh-sandbox-local + policy, but they have no in-repo file to anchor a positive claim against, so per the evidence backfill rule the claim is downgraded until an in-repo seam exists.
- <a id="dev-engine.schedule"></a>`engine.schedule` — downgrade: after_seconds / at / every_seconds (>=300s) supported; CC's cron-expression selectors are unsupported until the dsh-schedule selector seam lands (upstream extension planned).
- <a id="dev-engine.subagent-handoff"></a>`engine.subagent-handoff` — divergent: dsh-cc extension: the advisory-threshold contract (cc-handoff settings namespace, default 8192) is enforced only by the agents' prompt contract, not by the tool — put never rejects on size, and the cheap-lane shunt agents deliberately keep the pair out of their whitelists.
- <a id="dev-engine.tui"></a>`engine.tui` — downgrade: dsh --profile tui is the terminal surface: Shift+Tab mode cycling, Esc interrupt, Ctrl+C/Ctrl+S/Ctrl+T/Ctrl+O/Up/Tab bindings, approval previews with 1/2/3 answers on a shared FIFO, leading-! bash mode with separate history, TUI-local /export-md /copy /usage, six-role theme, context-occupancy statusline, custom statusLine and multi-hunk diff transcript rendering. Vim mode and ghost text are still later; the statusLine contract's remaining limits are detailed in ux.statusline.
- <a id="dev-engine.vendor-internals"></a>`engine.vendor-internals` — non-goal: Anthropic-internal or vendor-bound features; no design asset and out of parity scope.
- <a id="dev-engine.voice"></a>`engine.voice` — non-goal: Vendor feature with no design asset; won't port.
- <a id="dev-engine.web-fetch"></a>`engine.web-fetch` — downgrade: Mounted via @dsh-cc/tool-web-fetch (optional prompt summarized on resolve('haiku'); prompt without haiku hard-fails before fetch); the tool upgrades http: URLs to https: before the seam, CC-style. Fetch executor is the stock dsh-base @deepseek-ai/dsh-web-fetch-http provider with CC egress caps restated in cc-shell — upstream rc.1 resolves public-only addresses and pins them per redirect hop, closing the DNS-rebinding residual the retired literal gate left. WebFetch(domain:) content rules match hostnames. Residual: no Anthropic preflight, no 15-min cache, no pre-approved docs domains, no private-network escape hatch.
- <a id="dev-hooks.executors"></a>`hooks.executors` — downgrade: command + http (SSRF-allowlisted via allowedHttpHookUrls) executors are always on; prompt/agent executors are gated behind enablePromptHooks / enableAgentHooks (default off) and resolve their model: through ccModelRoutes (omitted model → the haiku cheap lane).
- <a id="dev-hooks.message-display"></a>`hooks.message-display` — downgrade: Not bridged — no dsh emit point for message-display rendering events.
- <a id="dev-hooks.notification"></a>`hooks.notification` — downgrade: Bridged for the permission_prompt subtype only; idle_prompt / auth_success / elicitation* subtypes have no equivalent seam in a headless harness (cannot map).
- <a id="dev-hooks.post-tool-batch"></a>`hooks.post-tool-batch` — downgrade: Not bridged — the dsh tool pipeline has no per-batch emit point yet (hook-bridge follow-up).
- <a id="dev-hooks.pre-compact"></a>`hooks.pre-compact` — upstream-blocked (upstream_dependency: precompact-seam): Needs an upstream compaction-waterfall interception seam in dsh-compaction (planned).
- <a id="dev-hooks.pre-tool-use"></a>`hooks.pre-tool-use` — downgrade: Bridged with matcher support and the permissionDecision decision contract, but additionalContext is ignored, bounding hook-based pre-tool shaping.
- <a id="dev-hooks.setup"></a>`hooks.setup` — downgrade: Bridged as a first-run approximation, not the full upstream Setup contract.
- <a id="dev-hooks.user-prompt-expansion"></a>`hooks.user-prompt-expansion` — downgrade: Not bridged — no dsh emit point for prompt-template expansion.
- <a id="dev-commands.agents"></a>`commands.agents` — downgrade: Slice 0 MVP: thin snapshot (list/detail/stop) over listChildren + agents + resumePinStore; groups are residency-only (Working / Idle / Ready — no Blocked/Done); /agents attach is a reserved, unimplemented namespace. The TUI local-slash surface consumes the same ccAgents snapshot and ADDS fold-derived decorations (provider, prompt excerpt, last stopReason) to the detail view; the thin preset surface vs the decorated TUI surface is a deliberate divergence, never different ids or ordering.
- <a id="dev-commands.branch"></a>`commands.branch` — downgrade: Forks and reports the child id; switching to the child requires a restart.
- <a id="dev-commands.config"></a>`commands.config` — downgrade: Text-only render/patch with an allowlisted key set, not an interactive editor.
- <a id="dev-commands.exit"></a>`commands.exit` — non-goal: Host-owned by design; the dsh-native equivalents are the TUI /exit command (packages/ui/tui/src/slash.ts) and the idle double Ctrl+C gesture, so a preset-side port is out of parity scope.
- <a id="dev-commands.init"></a>`commands.init` — downgrade: Drives a follow-up turn that writes/refreshes CLAUDE.md rather than the upstream one-shot initializer flow.
- <a id="dev-commands.model"></a>`commands.model` — non-goal: Host-owned by design; the dsh-native equivalents are the TUI-local /model and /effort commands (packages/ui/tui/src/slash.ts, model-catalog) plus the ccModelRoutes alias service, so a preset-side port is out of parity scope.
- <a id="dev-commands.plugin"></a>`commands.plugin` — downgrade: No interactive menu UI (text output only); a static trust-warning line instead of interactive consent prompts; CC-only subcommands not implemented (details/eval/init/prune/tag/validate, --config, --sparse, managed scope). State surface diverges from upstream: listing presents the dual-home merged view (claude home read-visible, dsh entries win per key), mutations write to the dsh home (`$DSH_HOME` / `~/.dsh`) never the Claude home, and a shadowed-claude marketplace re-add prints a one-line notice.
- <a id="dev-commands.resume"></a>`commands.resume` — downgrade: Lists sessions; switching is host-owned (dsh --resume <id>).
- <a id="dev-commands.rewind"></a>`commands.rewind` — upstream-blocked (upstream_dependency: session-file-snapshot-seam): Needs the session file-snapshot seam; no checkpoint/rewind design exists yet.
- <a id="dev-commands.tasks"></a>`commands.tasks` — downgrade: Human-facing /tasks lists background jobs only; the todo-list seam is pending. The model-facing dsh-tool-todo and the TUI Ctrl+T todo panel cover the rest of the workflow. The footer cross-links to /agents (N background agents — /agents for details) when the ccAgents snapshot service resolves.
- <a id="dev-sessions.checkpointing"></a>`sessions.checkpointing` — upstream-blocked (upstream_dependency: session-file-snapshot-seam): Feature absent; requires a snapshot seam in the session layer.
- <a id="dev-memory.claude-md-imports"></a>`memory.claude-md-imports` — downgrade: The @path import machinery is absent — no parser or loader handles imports today.
- <a id="dev-memory.core"></a>`memory.core` — downgrade: Per-repo isolation (worktrees/subdirs share one store), shared global layer, memory_save as the save channel, recall reference-doc suppression, and opt-in team memory all work; CC's mtime-threaded recall and memoryAge aging are deferred until the harness FsInfo seam grows an mtime field.
- <a id="dev-skills.system"></a>`skills.system` — downgrade: Loader + base tool-skill, CC paths conditional activation, bundled subset (debug/simplify/batch), and TUI /name routing all work; CC's verify/stuck skills are not ported (ant-only; verify companion files absent).
- <a id="dev-subagents.task-tool"></a>`subagents.task-tool` — divergent: subagent_type dispatch over per-workspace .claude/agents (persona = definition systemPrompt, alias routing via ccModelRoutes, sanitized toolFilter, maxDepth 3, bundled explore/dsh-cc-guide agents, the bundled explore additionally carrying read-only serena symbol retrieval tools mcp__serena__find_symbol / find_referencing_symbols / get_symbols_overview) plus the continuable background loop (run_in_background, agentId-addressable send_message/interrupt/list). Deliberate deviations: fork + background is rejected naming upstream issue #2124 (fork stays foreground one-shot); no TaskOutput alias and no outputFile field; omitting run_in_background stays foreground unless the definition pins background: true, unlike Claude Code's interactive omit=background. Pinned defaults (repo-guarded): the official plugin agent dsh-cc-agents:critic pins background: true, so omitting run_in_background on those Task calls launches them continuable in the background (the call resolves { status: 'async_launched' } immediately and the real result arrives later as a wake message — no inline result to compose on); run_in_background: false forces synchronous collection. Deliberate deviation on pins: dsh-cc-agents:executor and dsh-cc-agents:marathon ship NO pin — a mutating agent defaults to foreground so the delegator verifies its report before composing — and there is NO global omit=background default either: the bundled explore and dsh-cc-guide agents stay unpinned and keep foreground first-epoch collect. Foreground collect refit: non-fork foreground calls (named agents and general-purpose) collect the child's first epoch INLINE as a continuable child — same pin preallocation, pre-start pin write, and tombstone-on-throw as the background path, so a foreground-launched child is pinnable/resumable exactly like a background one. The collector is a dsh-cc-INTERNAL parity instrument (bus watcher over subagent/start + subagent/end, one shared listener pair and watch map, matched by first-epoch runId; docs/plans/ 2026-09-05-epoch-collector-dsh-cc.md), NOT a Claude Code mechanism — declared deviations: (1) for inline-collected epochs the settlement account lives only in the tool result, not the parent session log (the duplicated subagent-settled notice is dropped by a pop-once pre-step filter; a promoted child is removed from the set and delivers normally); (2) on exec.signal abort the child is interrupted aborted exactly once via ctx.subagents.interrupt({kind:'ancestor'}) and the tool call resolves PROMPTLY with a synthetic stopReason "aborted" (never awaiting child quiescence; the real terminal arrives only as the suppressed notice); (3) a seam without interrupt degrades to a prompt resolve without child interrupt. Output schema gains optional backgroundedByUser: true (set by the Slice 3 promotion path, never by Slice 2 code paths). Ctrl+B promotion (TUI-only surface): while the dialog is busy AND the root-realm ccCollectorRegistry (published by the cc-subagent-task plugin, CcPlugins pattern) has ≥1 armed foreground collect for THIS session AND CLAUDE_CODE_DISABLE_BACKGROUND_TASKS is unset, Ctrl+B promotes EVERY armed collect of the session: the pending tool call resolves { status: 'async_launched', agentId, backgroundedByUser: true }, the watch is released (the runId-matched end later resolves nothing), the child is REMOVED from the suppression set so its settlement notice delivers exactly once normally, and the TUI echoes one status line. Pre-acceptance promotion (Ctrl+B fired while startContinuable is still in flight) resolves async_launched as soon as start resolves. Repeated presses are idempotent (a promoted collect unregisters itself; the second press is a consumed no-op). Declared deviations: promotion is a TUI-only surface — non-TUI clients have NO promotion path; busy Esc/Ctrl+C still interrupts (every registered collect of the session aborts exactly-once with a prompt synthetic aborted resolution); idle Ctrl+B is untouched (the editor's tui.editor.cursorLeft binding); under tmux, Ctrl+B is the default prefix — double-press passes a literal Ctrl+B through (send-prefix) and the Ctrl+X Ctrl+B chord is P1 (not implemented). Env kill switch CLAUDE_CODE_DISABLE_BACKGROUND_TASKS: a non-empty value other than case-insensitive 0/false disables the backgrounding-by-default pins (omissions collect in foreground) — explicit run_in_background true/false arguments remain honored both ways. Deliberate partial-parity deviation: upstream Claude Code's exact precedence for this env var is unverified, so the argument precedence is declared rather than asserted as parity. Capacity guard: the Task tool refuses a new continuable child (background OR foreground collect) when the parent already has 25 live children (listChildren entries with activity running) with the actionable error "parent has 25 live subagents; /agents stop <id> to release one, or let children settle" — a dsh-cc-side safety valve; Claude Code has no equivalent surfaced limit. Continuable cold resume: each background spawn captures a resume pin (resolveDetailed-selected alias plus maxTokens) into the resumePinStore service (preset row cc-resume-pins, isolated as resumePinStore in cc-services), and resume restores the pinned tuple INCLUDING explicit absence (a pin recording an unset reasoningEffort/maxTokens is honored as absent rather than re-resolved); a pinned model the parent can no longer resolve is gated by the subagents-resume settings namespace (subagents-resume.onUnavailableModel: block\|route-current, default block) with deny codes SUBAGENT_MODEL_UNAVAILABLE / WORKSPACE_CHANGED / DEFINITION_CHANGED / PINNED_TOOL_UNAVAILABLE / PIN_ORPHANED / PIN_UNREADABLE naming the policy knob — Claude Code has no equivalent mechanism. Spawn-time preload (dsh-cc-side, no Claude Code equivalent): an explicit `tools:` entry that is still a deferred MCP tool is activated through the duck-typed ctx.toolSearch seam at spawn, BEFORE the child starts, on BOTH dispatch paths — activation is process-global (every admitting agent's schema grows after the spawn); wildcard entries are restrict-only, deny/sanitize-excluded names are never activated, eager names are skipped, and outcomes surface in the Task result text (Preloaded deferred tools for child / Not preloaded: name (reason)). Plugin-agent dispatch (dsh-cc-side, CC-parity): plugin agents are dispatched by CC-style scoped ids `plugin:agent` — file definitions and scoped ids occupy disjoint name spaces (file definitions whose agentType contains `:` are skipped with a warning so the scoped-id namespace stays unambiguous) and bare plugin names are not addressable, matching Claude Code. Plugin definitions are enumerated live from the `subagents` seam and folded identically to file definitions (persona, sanitized toolFilter, alias-resolved model, maxDepth 3, background pin, deferred-MCP preload, foreground collect / background continuable); dispatch targets the `spawn` provider, so the AgentProvider fork-inheritance semantics are not used. Mounting a plugin's agents without a namespacePrefix leaves them undiscoverable to the Task tool (back-compat escape hatch, not a supported dispatch mode). First-party exercise: the official dsh-cc-agents plugin (packages/plugin/dsh-cc-agents, shipped via the repo-root `dsh-cc` marketplace) dispatches by scoped id end-to-end — dsh-cc-agents:critic / dsh-cc-agents:executor mount from the real plugin dir and are pinned by plugin-agents.spec.ts and the loader's dsh-cc-agents.spec.ts. Delegation discipline is prompt-taught across the Task description, the background-loop system-prompt section, both spawn-result texts, the resume-pins send_message advisory and list_agents annotations, the repo CLAUDE.md, and the dsh-cc-agents orchestration skill: one task, one instance — send_message continues only the child's current assignment, and every new task is a fresh subagent_fork spawn.
- <a id="dev-mcp.config-discovery"></a>`mcp.config-discovery` — divergent: Deliberate dsh-first rule with no Claude Code analog: when a dsh-native config (the project .mcp.json or $DSH_HOME/.mcp.json) declares at least one server, Claude Code MCP config files are not loaded; skipped claude-only servers surface via a logger warn, a one-shot session-start TUI notice, and a self-clearing /mcp status line, and /mcp migrate imports them into $DSH_HOME/.mcp.json (raw entries verbatim, atomic write, .bak backup, existing names win). Escape hatch: mcpLoadClaudeFiles: true on cc-shell-glue restores the old all-merge behavior; an explicit mcpConfigFiles list bypasses gating entirely.
- <a id="dev-plugins.loader"></a>`plugins.loader` — divergent: Discovery presents the dual-home merged view: the Claude home ($CLAUDE_CONFIG_DIR / ~/.claude) stays read-visible for Claude Code compatibility while the dsh home ($DSH_HOME / ~/.dsh) is the write root, merged per key with dsh-wins. The fork is one-way (real Claude Code sees only its own home) and ids dsh has taken over stop tracking later claude-side changes; a claudeHome-alone caller keeps legacy single-root behavior.
- <a id="dev-plugins.management"></a>`plugins.management` — divergent: State root split: every /plugin mutation writes under the dsh home ($DSH_HOME / ~/.claude never written), the Claude home stays read-visible for compatibility, and both homes merge per key with dsh-wins (null tombstones hide claude-only marketplaces). Claude Code reads only its own home, so the fork is one-way; ids materialized into the dsh installed file stop tracking later claude-side changes (takeover staleness). Setting CLAUDE_CONFIG_DIR no longer relocates writes; a claudeHome-alone caller keeps legacy single-root behavior.
- <a id="dev-settings.migrations"></a>`settings.migrations` — downgrade: Mechanism only (version-gated runMigrations over an atomically-written settings.json, auto-run on mount) — no real migrations yet.
- <a id="dev-permissions.rules"></a>`permissions.rules` — downgrade: Rule engine + dangerous-command/path risk classifier, durable modes (default/acceptEdits/plan/auto/bypassPermissions via /permissions), plan deny with exit_plan_mode guidance, bypassPermissions pinning danger-full-access with resumeSandbox restore, and the always-allow derived-rule persistence all work. `auto` mode additionally gains an opt-in, escalate-only LLM risk-classifier stage: a one-shot auxiliary model verdict over tool name + rendered parameters (never tool results, input wrapped as an explicit DATA block the model is instructed never to repeat, quote, or follow), fail-to-ask on every classifier failure, `$defaults` soft-deny prose expansion, a verdict LRU cache, and durable `permission/classifier` session audit events (armed only when enabled, an llm service is mounted, and the alias route resolves). Read-only tool calls are exempt: they never invoke the classifier (the legacy auto path applies). Verdict parsing stays strict and fail-closed — a malformed output yields the constant reason "classifier output unparseable" (model output never reaches the modal; the audit record is digest-only). A per-route consecutive-failure circuit breaker (threshold 3, keyed provider/model) opens the stage for a failing lane: no further classifier calls on that route, one warn per process, one `breaker` audit event per session, reset by a settings change (rebuild). The breaker is restart-durable within a session: the first call of a session seeds the per-route streak from the durable log (attributed events only; a restored streak ≥ threshold opens at seed time). An open breaker surfaces one visible TUI fallback notice per session (live-appended `breaker` events only — never replayed logs), and caller-cancelled classifications are tagged `cancelled`, never breaker-counted. Raw model output is debuggable via the opt-in `DSH_PERMISSION_CLASSIFIER_DEBUG=1` process-log channel (`[dsh:classifier:raw]`, truncated to 2 KiB — never session events). Defaults: `timeoutMs` 8000, verdict budget 1024 tokens. The UI honors `permissions.defaultMode`: statusline, session switch, Shift+Tab cycle start, and the /permissions picker all fall back to the live merged settings default (fold precedence — a recorded session mode still wins), with the cycle clamped to its members. Settings-surface deviation: the auto-mode section is delivered as `permissions.autoMode` — an `autoMode` key inside the existing `permissions` namespace (camelCase key in a kebab namespace, like `defaultMode`), NOT Claude Code's root-level `autoMode` key, because top-level settings namespaces must be kebab-case upstream. Other documented CC auto-mode divergences (explicit ask rules still proxied to allow in auto; same-source allow-before-deny) remain recorded as unfixed follow-ups — they are not addressed or masked here. Remaining vs CC: managed/enterprise remote settings, UI mode cycle; the upstream permission/mode catalog pin is an upstream polish.
- <a id="dev-models.aliases"></a>`models.aliases` — downgrade: Frontmatter aliases (sonnet/opus/haiku/fable + dsh lanes + open set), settings model-aliases overlay with null-delete and builtin fallback, shared toAgentOptions per-field inheritance, and the ccModelRoutes service all work. Follow-ups: the /model command, ANTHROPIC_* env vars (no Anthropic semantics), and aliasing the main-session default model.
- <a id="dev-ux.statusline"></a>`ux.statusline` — downgrade: The command's stdout is rendered as multiple status-line rows capped at 3 (CC renders every row as its own status-line row); when the custom statusline is active, dsh-cc appends one client-drawn row below the command output carrying the permission mode + key hints (`<mode> · shift+tab · /quit`) — the stdin payload carries no permission-mode field (CC's doesn't either; CC renders that row as its own chrome). A 60 s hard run cap and a 64 KiB stdout cap are enforced (CC documents no timeout), and no debug-mode failure output is logged for the statusline command. The stdin payload omits fields with no truthful runtime source: version, output_style.name, worktree, workspace.git_worktree, workspace.repo, session_name, prompt_id, fast_mode, thinking, vim, agent, pr, cost.total_cost_usd / total_api_duration_ms / total_lines_* — context_window.current_usage IS emitted with CC field names (input_tokens / output_tokens / cache_creation_input_tokens / cache_read_input_tokens), truthfully sourced from the harness token usage projection's last-step buckets; steps cancelled or failed mid-flight may leave partial usage in that snapshot (documented staleness). used_percentage / remaining_percentage are derived from the currentUsage context length when present (fallback: the projected pressure sample; omitted when neither source exists). display_name duplicates model.id; exceeds_200k_tokens uses the currentUsage context length and is omitted when no occupancy source exists. transcript_path points at the dsh-cc-maintained CC-shape plaintext transcript mirror when it is ready (see ux.statusline-cc-transcript). hideVimModeIndicator is parsed but behaviorally inert (no vim mode yet); disableAllHooks / allowManagedHooksOnly gating keys are unimplemented. External settings.json edits apply at the next boot (no file watcher; in-process settings commits react immediately). refreshInterval is honored in seconds with a minimum clamp of 1.
- <a id="dev-ux.statusline-cc-transcript"></a>`ux.statusline-cc-transcript` — divergent: When the custom statusline is active, dsh-cc maintains a CC-shape plaintext transcript mirror at $DSH_HOME/tui/cc-transcripts/<sessionId>.jsonl (assistant usage lines + user anchor lines, atomic rebuild on bind/rebind, seq-watermarked appends) and the statusline payload's transcript_path advertises the mirror only once it is ready (>=1 usage line), instead of the dsh zstd-compressed private-format session artifact that CC-shape consumers cannot parse. The mirror is a filtered projection (usage + anchor lines only), not a full transcript. Hook payloads (packages/hooks/hooks-claude-code/src/payloads.ts) still point transcript_path at the dsh session artifact.

## Upstream dependencies

### session-file-snapshot-seam — Session file-snapshot seam

**Problem:** dsh sessions persist a jsonl/sqlite projection; there is no per-prompt snapshot store of edited files to anchor a rewind against.

**Claude Code contract:** Every prompt checkpoints files about to be edited; /rewind (or Esc Esc) restores code, conversation, or both; checkpoints persist across resumed sessions and do not cover Bash-side changes.

**Refs:**
- https://code.claude.com/docs/en/checkpointing

### schedule-cron-selector — Cron-expression selector for dsh-schedule

**Problem:** dsh-schedule supports after_seconds / at / every_seconds (>=300s) but has no cron-expression selector, so ScheduleCronTool parity is unreachable.

**Claude Code contract:** Claude Code scheduled reminders accept cron expressions as selectors.

### precompact-seam — PreCompact interception seam in dsh-compaction

**Problem:** dsh-compaction has no interception point where a user-configured hook could run before compaction fires, so the PreCompact event cannot bridge.

**Claude Code contract:** Claude Code fires PreCompact hooks before automatic or manual compaction, letting hooks inject or transform context.

### tasks-todo-seam — Human-facing todo-list seam for /tasks

**Problem:** dsh-tool-todo is model-facing and the TUI renders the todo panel, but human-facing /tasks still lists background jobs only.

**Claude Code contract:** Claude Code's /tasks surfaces the session todo list to the human user.

### memory-fsinfo-mtime — Memory freshness — FsInfo mtime seam

**Problem:** CC threads mtimeMs through recall and ages memories (memoryAge); the current dsh-fs seam does not expose mtime, so both are deferred.

**Claude Code contract:** Claude Code memory recall orders by file mtime and exposes a memoryAge signal so stale memories age out of prominence.

### notification-seam — Notification seam in deepseek-harness

**Problem:** No notification seam exists in deepseek-harness (bell/OS/iterm); shipping notifications needs a new design.

**Claude Code contract:** Claude Code emits desktop/terminal notifications for attention-needed moments (bell, OS notification, iTerm2 escape sequences).

### permission-mode-catalog-pin — Upstream catalog pin of permission/mode session events

**Problem:** The plugin registers permission/mode into KNOWN_SESSION_EVENT_TYPES via a runtime Set.add at load; bumping the harness's own compiled catalog is an upstream polish.

**Claude Code contract:** Permission-mode session state persists across process restarts, not just within the registering process.

### upstream-issue-2124 — Upstream harness issue

**Problem:** Fork children remain foreground one-shot until upstream harness issue #2124 resolves; fork + run_in_background is rejected with an error naming the issue.

**Claude Code contract:** Claude Code Task supports the fork provider in continuable background mode with the same agentId-addressable loop as spawn.

## Renamed capabilities

_None._

---

Baseline sources: cc-docs (https://code.claude.com/docs), cc-repo (https://github.com/anthropics/claude-code).
Newest upstream retrieval: 2026-09-05.
Freshness threshold: 120 days — re-verify refs older than that before trusting a row.
